"""Temporal soil engine: pure, clock-injected moisture interpretation.

The modules here are free of Home Assistant imports so the roadmap timeline
scenarios run as ordinary domain tests with an injected ``now``.
"""
